apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: default
type: Opaque
stringData:
  db-host: "postgres-service"
  db-port: "5432"
  db-name: "platform_db"
  db-user: "postgres"
  db-password: "CHANGE_ME_IN_PRODUCTION"
  encryption-master-key: "CHANGE_ME_BASE64_ENCODED"
  encryption-salt: "CHANGE_ME_BASE64_ENCODED"
